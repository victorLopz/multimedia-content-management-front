import React from 'react'

export const Title = ({title}) => {
  return (
    <div className="intro-y flex items-center mt-8">
      <h2 className="text-lg mr-auto font-semibold">{title}</h2>
    </div>
  )
}
